// mocks/server.ts
export const server = {
  listen: () => {
    // démarrage du mock serveur (optionnel)
  },
  close: () => {
    // arrêt du mock serveur (optionnel)
  },
  resetHandlers: () => {
    // reset des handlers mockés
  },
};
